<div>
  {% include 'top_menu' %}
  <div class="gallery_page py-8 lg:py-20">
    <div class="container">
      <div class="flex flex-col items-center">
        <h1 class="md_big_title font-bold text-center">
          <span class="color_primary">May Yang’s </span>Gallery
        </h1>
        <nav>
          <ul
            class="
              text-center
              md:text-left
              gallery-tabs-list
              flex
              text-xs
              md:text-base
              lg:text-2xl
              font-bold
              my-8
              lg:my-12
            "
          >
            <li class="gallery-tab" id="tab1">MY Travel</li>
            <li class="gallery-tab" id="tab2">MY Events</li>
            <li class="gallery-tab" id="tab3">MY Studio</li>
            <li class="gallery-tab" id="tab4">MY Juniors</li>
          </ul>
        </nav>

        <div class="content-section">
          <div class="gallery-tab-content"></div>
        </div>
      </div>
    </div>
  </div>
</div>

<style>
  .gallery_page {
  }

  .gallery_page .container {
  }

  .gallery_page .gallery-tab {
    margin-right: 4rem;
    cursor: pointer;
    list-style: none;
		color: #76797e;
  }

  .gallery_page .gallery-tab:last-child {
    margin-right: 0;
  }

  .gallery-tab.active {
    color: #2bbcbe;
    position: relative;
  }

  .gallery-tab.active:after {
    content: '';
    position: absolute;
    bottom: -5px;
    left: 0;
    right: 0;
    background: #2bbcbe;
    height: 3px;
    border-radius: 2px;
  }

  /* Gallery Modal */

  .gallery-image-container.show {
    display: block;
  }

  .gallery-image-container.remove {
    display: none;
  }

  .gallery-modal {
    pointer-events: none;
    opacity: 0;
    transition: opacity 0.3s;
    position: fixed;
    top: 50px;
    left: 0;
    right: 0;
    width: 900px;
    max-width: 100%;
    margin: 0 auto;
    border-radius: 4px;
    box-shadow: 0 3px 10px rgba(0, 0, 0, 0.3);
  }

  .gallery-modal-btn {
    width: 100%;
    height: 350px;
  }

  .modal-close-btn {
    position: absolute;
    right: 16px;
    font-size: 50px;
    color: white;
  }

  .gallery-modal-overlay {
    pointer-events: none;
    opacity: 0;
    transition: opacity 0.3s;
    position: fixed;
    inset: 0;
    width: 100%;
    height: 100%;
    background-color: rgba(0, 0, 0, 0.3);
  }

	.juniors-first-image {
		object-position: -200px 50%;
	}

  /** JS */
  .gallery_page .is-visible {
    opacity: 1;
    pointer-events: auto;
  }

  @media only screen and (max-width: 1024px) {
    .gallery_page .gallery-tab {
      margin-right: 2rem;
    }

    .gallery-modal {
      max-width: 700px;
    }
  }

  @media only screen and (max-width: 600px) {
    .gallery_page .gallery-tab {
      margin-right: 1rem;
    }
  }
</style>
